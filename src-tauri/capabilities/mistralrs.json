{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mistralrs",
  "description": "Permissions for the mistral.rs local inference engine window",
  "windows": ["main"],
  "permissions": ["mistralrs:default"]
}
